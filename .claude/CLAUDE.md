Read `HACKING.md` before doing any changes in this repo.

Build `check_style` target to check style. Always check style after your changes.

Build `Run_AllTests_Headless_Parallel` target to test your changes. Always run tests after your changes.

When adding comments, add them in doxygen format. Don't forget to document function arguments and return value.
