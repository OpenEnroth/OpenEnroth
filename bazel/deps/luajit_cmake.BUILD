# luajit-cmake is a CMake helper module for building LuaJIT.
# In the Bazel build, LuaJIT is built directly via rules_foreign_cc make(),
# so this repo is only kept for reference. No build targets needed.
