name: Linux (Flatpak)

on:
  pull_request: null
  workflow_dispatch:
    inputs:
      myCommit:
        description: Commit SHA1
        required: false
        default: ''
        type: string
      releaseTag:
        description: Release tag
        required: false
        default: ''
        type: string
  workflow_call:
    inputs:
      myCommit:
        description: Commit SHA1
        required: false
        default: ''
        type: string
      releaseTag:
        description: Release tag
        required: false
        default: ''
        type: string
  release:
    types:
      - published

# A new push into a PR auto-cancels the run for the previous one. Master and nightly unaffected.
concurrency:
  group: linux-flatpak-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

env:
  CCACHE_COMPILERCHECK: content
    
jobs:
  build_flatpak_bundle:
    timeout-minutes: 60
    runs-on: ubuntu-24.04
    container:
      image: bilelmoussaoui/flatpak-github-actions:freedesktop-24.08
      options: --privileged
    strategy:
      fail-fast: false
      matrix:
        configuration:
          - RelWithDebInfo
        architecture:
          - x86_64

    steps:
    - name: make ccache dir
      run: |
        mkdir -p .ccache
        cd .ccache
        echo "CCACHE_DIR=$PWD" >> $GITHUB_ENV
        
    - name: Checkout
      uses: actions/checkout@v5.0.0
      with:
        submodules: recursive
        ref: '${{inputs.myCommit}}'
          
    - name: Run ccache
      uses: hendrikmuhs/ccache-action@v1.2.18
      with:
        key: 'linux-flatpak-${{matrix.configuration}}-${{matrix.architecture}}'
        verbose: 2
        max-size: 400M
        save: ${{github.event_name != 'pull_request'}}
        
    - name: Run Flatpak build
      uses: flatpak/flatpak-github-actions/flatpak-builder@dcbd3a30d991a38708c318d17f5352cc1268c215
      with:
        run-tests: true
        bundle: io.github.openenroth.openenroth_${{inputs.releaseTag}}_${{matrix.configuration}}_${{matrix.architecture}}.flatpak
        manifest-path: distribution/linux/flatpak/dev.io.github.openenroth.openenroth.yml
        arch: ${{matrix.architecture}}
        verbose: true
        cache: true
        restore-cache: false
        upload-artifact: false

    - name: Annotate failure log
      if: failure()
      run: |
        f=$(ls -t .flatpak-builder/build/openenroth*/bazel-*.log 2>/dev/null | head -1)
        if [ -n "$f" ]; then
          grep -E "error:|ERROR:|CMake Error|Error [0-9]|FAILED:|incomplete" "$f" | head -10 | while IFS= read -r l; do echo "::warning::${l:0:400}"; done
        else
          echo "::warning::no bazel log found; failed before bazel ran"
          ls .flatpak-builder/build 2>/dev/null | head -5 | while read d; do echo "::warning::builddir: $d"; done
        fi

    - name: Publish release
      if: inputs.releaseTag != ''
      uses: softprops/action-gh-release@v2.0.8
      with:
        prerelease: true
        tag_name: '${{inputs.releaseTag}}'
        files: io.github.openenroth.openenroth_${{inputs.releaseTag}}_${{matrix.configuration}}_${{matrix.architecture}}.flatpak

    - name: Cleanup ccache
      run: |
        ccache -c
